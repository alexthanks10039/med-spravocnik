const views=document.querySelectorAll('.view');const title=document.querySelector('#title');document.querySelectorAll('nav button').forEach(button=>button.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));button.classList.add('active');views.forEach(x=>x.classList.remove('active'));document.querySelector(`#${button.dataset.view}`).classList.add('active');title.textContent=button.textContent;if(button.dataset.view==='rag')loadRag();});
async function loadRag(){const q=document.querySelector('#ragQuery').value;const space=document.querySelector('#ragSpace').value;const params=new URLSearchParams({q});if(space)params.set('space',space);const data=await fetch(`/api/rag?${params}`).then(r=>r.json());document.querySelector('#ragResults').innerHTML=data.items.map(x=>`<article class="rag-card"><small>${label(x.space)}</small><h3>${x.title}</h3><p>${x.summary}</p><div class="tags">${x.tags.map(t=>`<span>${t}</span>`).join('')}</div></article>`).join('')||'<p>Ничего не найдено</p>';}
function label(x){return({commercial:'Коммерческое',development:'Разработка',content:'Контент'})[x]||x}document.querySelector('#ragQuery').oninput=loadRag;document.querySelector('#ragSpace').onchange=loadRag;document.querySelector('#searchButton').onclick=()=>{document.querySelector('#ragQuery').value=document.querySelector('#globalSearch').value;document.querySelector('button[data-view="rag"]').click();};document.querySelector('#calculate').onclick=async()=>{const data=await fetch('/api/calculators/bmi',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({weightKg:Number(document.querySelector('#weight').value),heightCm:Number(document.querySelector('#height').value)})}).then(r=>r.json());document.querySelector('#calcResult').textContent=data.value?`${data.value} ${data.unit} · ${data.category}`:data.message;};
