import { Router } from 'express'; import { z } from 'zod'; import { prisma } from '../../shared/prisma.js'; import { adminMiddleware,authMiddleware } from '../../shared/middleware/auth.middleware.js'; import { AppError } from '../../shared/middleware/error.middleware.js';
export const diseasesRouter=Router(); const schema=z.object({name:z.string().min(2),icd10:z.string().optional(),symptoms:z.string().optional(),diagnostics:z.string().optional(),treatment:z.string().optional()});
diseasesRouter.get('/',async(req,res,next)=>{try{const q=String(req.query.q||'');res.json(await prisma.disease.findMany({where:q?{OR:[{name:{contains:q}},{icd10:{contains:q}},{symptoms:{contains:q}},{treatment:{contains:q}}]}:undefined,orderBy:{name:'asc'}}));}catch(e){next(e);}});
diseasesRouter.get('/:id',async(req,res,next)=>{try{const x=await prisma.disease.findUnique({where:{id:req.params.id}});if(!x)throw new AppError('Disease not found',404);res.json(x);}catch(e){next(e);}});
diseasesRouter.post('/',authMiddleware,adminMiddleware,async(req,res,next)=>{try{res.status(201).json(await prisma.disease.create({data:schema.parse(req.body)}));}catch(e){next(e);}});
