import { Router } from 'express'; import { z } from 'zod'; import { prisma } from '../../shared/prisma.js'; import { adminMiddleware,authMiddleware } from '../../shared/middleware/auth.middleware.js'; import { AppError } from '../../shared/middleware/error.middleware.js';
export const articlesRouter=Router(); const schema=z.object({title:z.string().min(2),slug:z.string().min(2),category:z.string().min(2),description:z.string().optional(),content:z.string().min(2),tags:z.array(z.string()).default([]),isPublished:z.boolean().default(true)});
articlesRouter.get('/',async(req,res,next)=>{try{const q=String(req.query.q||'');res.json(await prisma.article.findMany({where:{isPublished:true,OR:q?[{title:{contains:q}},{description:{contains:q}},{content:{contains:q}}]:undefined},orderBy:{updatedAt:'desc'}}));}catch(e){next(e);}});
articlesRouter.get('/:id',async(req,res,next)=>{try{const x=await prisma.article.findUnique({where:{id:req.params.id}});if(!x||!x.isPublished)throw new AppError('Article not found',404);res.json(x);}catch(e){next(e);}});
articlesRouter.post('/',authMiddleware,adminMiddleware,async(req,res,next)=>{try{const d=schema.parse(req.body);res.status(201).json(await prisma.article.create({data:{...d,tags:JSON.stringify(d.tags)}}));}catch(e){next(e);}});
